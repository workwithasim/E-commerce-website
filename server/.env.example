DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/restaurant_db?schema=public"
PORT=5000
CLIENT_ORIGIN="http://localhost:5173"
# Set long random secrets for stable sessions across server restarts.
JWT_SECRET=""
REFRESH_SECRET=""
# Optional: select a default tenant; otherwise the first active tenant is used.
DEFAULT_TENANT_SLUG=""
